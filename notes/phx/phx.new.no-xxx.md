  * `--no-assets` - equivalent to `--no-esbuild` and `--no-tailwind`
  * `--no-dashboard` - do not include Phoenix.LiveDashboard
  * `--no-ecto` - do not generate Ecto files
  * `--no-esbuild` - do not include esbuild dependencies and assets. We do
  * `--no-gettext` - do not generate gettext files
  * `--no-html` - do not generate HTML views
  * `--no-live` - comment out LiveView socket setup in your Endpoint and
  * `--no-mailer` - do not generate Swoosh mailer files
  * `--no-tailwind` - do not include tailwind dependencies and assets. The
  * `--no-version-check` - skip the version check for the latest phx_new
When passing the `--no-ecto` flag, Phoenix generators such as `phx.gen.html`,
access. In those cases, you can pass the `--no-context` flag to generate most
Similarly, if `--no-html` is given, the files generated by `phx.gen.html` will
with the `--no-install` flag.
